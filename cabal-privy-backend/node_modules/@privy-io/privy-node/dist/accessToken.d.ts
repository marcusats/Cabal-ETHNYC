import crypto from 'crypto';
import { AccessTokenClaims } from './model/data';
/**
 * Signs access token claims with the provided signing key, returning a JWT string.
 */
export declare const signAccessToken: (signingKey: crypto.KeyObject, claims: AccessTokenClaims) => Promise<string>;
/**
 * Returns access token claims for the given data requester.
 */
export declare const createAccessTokenClaims: (apiKey: string, requesterId: string) => AccessTokenClaims;
/**
 * Returns the JWT signing key generated deterministically from the API secret.
 */
export declare const jwtKeyFromApiSecret: (apiSecret: string) => crypto.KeyObject;
